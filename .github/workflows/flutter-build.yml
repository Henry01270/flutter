name: Flutter APK Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build Flutter APK
    runs-on: ubuntu-latest

    steps:
      - name: 🔹 Checkout Repository
        uses: actions/checkout@v3

      - name: 🔹 Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.0'

      - name: 🔹 Check Flutter Environment
        run: flutter doctor

      - name: 🔹 Change to Flutter project directory
        run: cd my_app

      - name: 🔹 Install dependencies
        run: flutter pub get
        working-directory: my_app

      - name: 🔹 Clean project (Fix Gradle issues)
        run: flutter clean
        working-directory: my_app

      - name: 🔹 Build APK
        run: flutter build apk --release
        working-directory: my_app

      - name: 🔹 Check if APK exists
        run: ls -al my_flutter_project/build/app/outputs/flutter-apk/

      - name: 🔹 Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: flutter-apk
          path: my_flutter_project/build/app/outputs/flutter-apk/app-release.apk
